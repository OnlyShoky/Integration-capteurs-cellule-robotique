cd ~/ariac_ws

% on source
source install/setup.bash

% On lance gazebo
gnome-terminal --tab -e "rosrun osrf_gear gear.py --development-mode -f src/tprobotique/data/planeta.yaml"

% on lance la simulation
gnome-terminal --tab -e "rosservice call /ariac/start_competition"

% on lance le programme
gnome-terminal --tab -e "rosrun tprobotique main.py"


% Il manque Faire le rapport , readme
% il detecte bien le dernier objet [0] et il choisi bien le bac 
% Manque divisé le fichier de config en 2, et faire plusieur type de config initial
% on utilise que 9 camera
